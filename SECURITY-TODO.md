# SECURITY-TODO — إصلاحات أمنية مؤجّلة (تحتاج موافقة المالك)

هذه القائمة تتابع المشكلات الأمنية التي رُصدت في جولة التنظيف. البنود
المعلَّمة ✅ نُفِّذت في جولات لاحقة (مع تشغيل lint واختبارات التدفّق والبناء)، أمّا
البقية فتحتاج قراراً أو إجراءً تشغيلياً من المالك.

---

## 1) حسابات دخول ثابتة مضمّنة في الكود (`fixedTeacherLogins`) — ✅ عُولِج

**الموقع:** `server.ts` (الدالة `fixedTeacherLogins`).

**ما نُفِّذ:** أُزيلت الهاشات المضمّنة نهائياً من الكود وأصبحت تُقرأ من متغيّرات
بيئة عبر `envBootstrapPasswordHash(...)`:
- `ah.alfailakawi@paaet.edu.kw` (admin) → `process.env.MIRAS_ADMIN_PASSWORD_HASH`
- `ada.alenezi@paaet.edu.kw` (teacher) → `process.env.MIRAS_TEACHER_PASSWORD_HASH`

عند غياب المتغيّر يُستخدم هاش «حارس» لا يطابق أي كلمة مرور (يبدأ بـ`sha256:` فلا
يسقط إلى مقارنة نصّ صريح)، فيتعطّل الحساب حتى يُزوّده المشرف. راجع `.env.example`.

**كيف يدخل المشرف بعد ذلك:** يولّد هاش كلمة مروره الجديدة ويضعه في المتغيّر:
`node -e "console.log(require('crypto').createHash('sha256').update(process.argv[1]).digest('hex'))" "كلمة-المرور"`
ثم يضبط `MIRAS_ADMIN_PASSWORD_HASH` (و`MIRAS_TEACHER_PASSWORD_HASH` للمعلّمين) في
أسرار Cloud Run، ويدخل بكلمة المرور الأصلية.

**يبقى مطلوباً:** تدوير كلمة المرور فعلياً (اختيار كلمة جديدة قوية) + **تنظيف
تاريخ git** لإزالة الهاشات القديمة من التاريخ (انظر البند 4).

## 2) حسابات معلّمين حقيقية مضمّنة في `src/server/db.ts` — ✅ عُولِج

**الموقع:** `src/server/db.ts` (`initialTeachers`).

**ما نُفِّذ:** الهاش المضمّن المشترك أُزيل من الكود، وأصبحت الحسابات الثلاثة
تُبذَر بهاش يُقرأ من `process.env.MIRAS_TEACHER_PASSWORD_HASH` (مع نفس منطق
الحارس عند الغياب). هذا الهاش القديم كان `sha256` لكلمة مرور صارت مكشوفة، لذا
**يجب تدوير كلمة المرور** وضبط المتغيّر بقيمة جديدة.

**يبقى مطلوباً:** تدوير كلمة المرور + **تنظيف تاريخ git** لإزالة الهاش القديم
من التاريخ (البند 4). ملاحظة: قواعد البيانات المُهيّأة مسبقاً (`data/db.json`
في الإنتاج) قد تحمل الهاش القديم — يجب تحديثها أيضاً عند التدوير.

## 3) بيانات شخصية (PII) وأسرار حُذفت من الشجرة لكنها باقية في تاريخ git — حرِج

حُذفت في هذه الجولة ملفات كانت تحوي أسراراً/بيانات شخصية حقيقية، لكنّ **حذف
الملف من الشجرة لا يمسحه من تاريخ git**. الأسرار التالية لا تزال قابلة
للاسترجاع من التاريخ:

- كلمة المرور الصريحة `***REDACTED***` (كانت في `hash.cjs`, `test-login*.{cjs,mjs}`,
  `flows.main.mjs` بالجذر، واختبارات التدفّق قبل التعقيم).
- هاش scrypt لحساب حقيقي في `check-pw.cjs` (سكربت كسر كلمة مرور).
- أسماء وبُرُد معلّمين حقيقية في `update-db.cjs` و `patch-json.mjs`.
- الهاش `sha256:15ed79e0…6928d678` عبر ملفات متعدّدة.

**المطلوب:** إعادة كتابة تاريخ git (مثل `git filter-repo` أو BFG) لإزالة هذه
القيم، ثم `force-push` — **بموافقة المالك فقط** ومع تنسيق مع كل من لديه نسخة
من المستودع. لم تُعد كتابة التاريخ في هذه الجولة (القاعدة تمنع ذلك بلا موافقة).

## 4) قاعدة البيانات "كل شيء في الذاكرة" تتطلّب `max-instances=1` — ⚠️ يبقى إجراءً تشغيلياً على المالك

الخادم يحمّل الحالة في الذاكرة داخل العملية. تشغيل أكثر من نسخة (instance)
سيؤدّي إلى **حالة منقسمة/غير متّسقة** بين النسخ (وتضارب في الكتابة إلى
`data/db.json`).

**لماذا لم يُنفَّذ في الكود:** لا يوجد في المستودع أي ملف نشر لخدمة Cloud Run
(`miras-api`) — لا `cloudbuild.yaml` ولا `service.yaml` ولا سكربت `gcloud run
deploy`. `Dockerfile` يبني الصورة فقط ولا يملك سلطة على عدد النسخ، و
`.github/workflows/firebase-hosting-deploy.yml` ينشر الاستضافة فقط
(`--only hosting:mirasedu`). الخدمة تُنشر خارج المستودع، فلم يُخترَع ملف نشر
جديد لهذا الغرض.

**ما نُفِّذ:** وُثِّق الإجراء في `README.md` (قسم _Cloud Run API service_) مع
الأمر الواجب تنفيذه مرّة واحدة:

```sh
gcloud run services update miras-api --region us-central1 --max-instances=1
```

**المطلوب من المالك:** تنفيذ الأمر أعلاه (أو ضبط الحد من واجهة Cloud Run)،
والتأكّد من تثبيته في أي مسار نشر مستقبلي.

## 5) CORS مفتوح بالكامل — ✅ عُولِج

**الموقع:** `server.ts` — الوسيط `app.use(cors(...))` (حوالي السطر 8862) مع
الدالتين `mirasCorsAllowedOrigins()` و`mirasCorsOriginAllowed()`.

**ما نُفِّذ:** أُزيل `app.use(cors())` المفتوح واستُبدل بقائمة أصول مسموحة:

- نطاقات المشروع على Firebase Hosting (`mirasedu.web.app`,
  `mirasedu.firebaseapp.com`, `meras-320eb.web.app`,
  `meras-320eb.firebaseapp.com`)؛
- ما يُشتق من `APP_URL` / `MIRAS_PUBLIC_APP_URL` / `MIRAS_PUBLIC_LOGIN_ORIGINS`؛
- المتغيّر الجديد **`MIRAS_ALLOWED_ORIGINS`** (نطاقات مفصولة بفواصل) للنطاق
  الرسمي أو نطاقات إضافية؛
- `localhost` / `127.0.0.1` **في التطوير والاختبار فقط** (يُمنع في الإنتاج عبر
  `isProductionLikeRuntime()`).

الطلبات بلا ترويسة `Origin` (curl، فحوص الصحة، اختبارات التدفّق) تمرّ كما هي —
CORS يخصّ المتصفح وحده. المسار الطبيعي للتطبيق same-origin عبر إعادة كتابة
Hosting (`/api/**` → Cloud Run) فلا يتأثّر. راجع `.env.example`.

## 6) `MIRAS_SESSION_SECRET` له قيمة احتياطية مشتقّة — ✅ عُولِج

**الموقع:** `server.ts` — `resolveMirasSessionSecret()` (حوالي السطر 142).

**ما نُفِّذ:** أُسقطت القيمة الاحتياطية المشتقّة من
`sha256("miras-local-" + process.cwd())` نهائياً. السلوك الآن:

- في تشغيل شبيه بالإنتاج (`NODE_ENV=production` أو وجود
  `K_SERVICE` / `FIREBASE_CONFIG` / `GOOGLE_CLOUD_PROJECT`): يُطبع خطأ واضح
  يشرح كيف يُولَّد السرّ ثم **يتوقّف الإقلاع** بـ`process.exit(1)` (fail-closed)؛
- في التطوير والاختبار: يُولَّد سرّ عشوائي لكل عملية مع تحذير، فتبقى
  `npm run test:flows` تعمل دون ضبط المتغيّر (الجلسات تُبطَل عند كل إعادة تشغيل
  فقط).

**المطلوب من المالك:** ضبط `MIRAS_SESSION_SECRET` بقيمة عشوائية قوية في أسرار
Cloud Run قبل النشر التالي، وإلا لن تُقلع الخدمة:
`node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`

---

_أُنشئ هذا الملف ضمن جولة تنظيف آمنة لم تُعدّل أي منطق تشغيل. البنود المتبقّية
تحتاج قرار المالك قبل التنفيذ._
