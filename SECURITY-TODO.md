# SECURITY-TODO — إصلاحات أمنية مؤجّلة (تحتاج موافقة المالك)

هذه القائمة تسرد مشكلات أمنية **لم تُطبَّق** في جولة التنظيف لأنها قد تكسر
التشغيل أو تقفل الدخول أو تتطلّب إعادة كتابة تاريخ git. كلّها تحتاج قراراً من
المالك. لم تُلمَس ملفات التشغيل (`server.ts`, `src/server/db.ts`) في هذه الجولة.

---

## 1) حسابات دخول ثابتة مضمّنة في الكود (`fixedTeacherLogins`) — حرِج

**الموقع:** `server.ts:14532` (الدالة `fixedTeacherLogins`)، والهاشات عند
`server.ts:14539` و `server.ts:14547`.

- `ah.alfailakawi@paaet.edu.kw` (role: **admin**) — `passwordHash: sha256:2a57b6d3…91cc25`
- `ada.alenezi@paaet.edu.kw` (role: teacher) — `passwordHash: sha256:15ed79e0…6928d678`

**لماذا لم يُحذف:** حذف هذه الكتلة قد **يقفل دخول المشرف/المعلّم الحقيقي**.

**المطلوب من المالك (بالترتيب):**
1. **تدوير كلمة مرور** حساب المشرف `ah.alfailakawi@paaet.edu.kw` فوراً.
2. نقل `fixedTeacherLogins` بالكامل إلى **متغيّرات بيئة** (لا هاشات مضمّنة في الكود).
3. **تنظيف تاريخ git** (انظر البند 4) لإزالة الهاشات القديمة من التاريخ.

## 2) حسابات معلّمين حقيقية مضمّنة في `src/server/db.ts` — حرِج

**الموقع:** `src/server/db.ts:833‑835` — ثلاثة حسابات بأسماء وبُرُد حقيقية
(`Ah.Alfailakawi@paaet.edu.kw`, `dr.ahmad.alfailakawi@gmail.com`,
`ada.alenezi@paaet.edu.kw`) وكلّها `passwordHash: sha256:15ed79e0…6928d678`.

**خطورة مؤكّدة:** هذا الهاش هو `sha256('***REDACTED***')` — أي أنّ كلمة المرور
`***REDACTED***` كانت مكتوبة بنص صريح في المستودع (في سكربتات حُذفت الآن)، فأصبحت
كلمة مرور هذه الحسابات **معروفة/مكشوفة فعلياً**.

**المطلوب:** تدوير كلمة مرور هذه الحسابات فوراً، ثم نقل بيانات البذر الأوّلي
إلى متغيّرات بيئة/تهيئة خارجية بدل تضمينها في الكود، ثم تنظيف التاريخ.
لم يُعدّل `src/server/db.ts` (منطق تشغيل — يحتاج موافقة).

## 3) بيانات شخصية (PII) وأسرار حُذفت من الشجرة لكنها باقية في تاريخ git — حرِج

حُذفت في هذه الجولة ملفات كانت تحوي أسراراً/بيانات شخصية حقيقية، لكنّ **حذف
الملف من الشجرة لا يمسحه من تاريخ git**. الأسرار التالية لا تزال قابلة
للاسترجاع من التاريخ:

- كلمة المرور الصريحة `***REDACTED***` (كانت في `hash.cjs`, `test-login*.{cjs,mjs}`,
  `flows.main.mjs` بالجذر، واختبارات التدفّق قبل التعقيم).
- هاش scrypt لحساب حقيقي في `check-pw.cjs` (سكربت كسر كلمة مرور).
- أسماء وبُرُد معلّمين حقيقية في `update-db.cjs` و `patch-json.mjs`.
- الهاش `sha256:15ed79e0…6928d678` عبر ملفات متعدّدة.

**المطلوب:** إعادة كتابة تاريخ git (مثل `git filter-repo` أو BFG) لإزالة هذه
القيم، ثم `force-push` — **بموافقة المالك فقط** ومع تنسيق مع كل من لديه نسخة
من المستودع. لم تُعد كتابة التاريخ في هذه الجولة (القاعدة تمنع ذلك بلا موافقة).

## 4) قاعدة البيانات "كل شيء في الذاكرة" تتطلّب `max-instances=1` — يحتاج مراجعة

الخادم يحمّل الحالة في الذاكرة داخل العملية. تشغيل أكثر من نسخة (instance)
سيؤدّي إلى **حالة منقسمة/غير متّسقة** بين النسخ (وتضارب في الكتابة إلى
`data/db.json`). يجب ضبط النشر على **نسخة واحدة كحد أقصى** (مثلاً
`--max-instances=1` على Cloud Run، أو ما يعادله) حتى تُعاد هندسة التخزين إلى
مخزن مشترك. مراجعة منفصلة — لم يُنفّذ.

## 5) CORS مفتوح بالكامل — يحتاج مراجعة

**الموقع:** `server.ts:8781` — `app.use(cors());` بلا قائمة أصول مسموحة، أي
يسمح لأي أصل (origin). يجب تقييده بقائمة نطاقات معروفة. مراجعة منفصلة — لم يُنفّذ.

## 6) `MIRAS_SESSION_SECRET` له قيمة احتياطية مشتقّة — يحتاج مراجعة

**الموقع:** `server.ts:136‑138`. عند غياب متغيّر البيئة `MIRAS_SESSION_SECRET`
يُشتقّ سرّ الجلسات من `sha256("miras-local-" + process.cwd())` — وهي قيمة
**قابلة للتخمين** ومشتركة بين أي تشغيلات لها نفس مسار العمل، ما يُضعف توقيع
الجلسات في الإنتاج. يجب فرض ضبط `MIRAS_SESSION_SECRET` (عشوائي قوي) وإسقاط
القيمة الاحتياطية في الإنتاج. مراجعة منفصلة — لم يُنفّذ.

---

_أُنشئ هذا الملف ضمن جولة تنظيف آمنة لم تُعدّل أي منطق تشغيل. البنود أعلاه
تحتاج قرار المالك قبل التنفيذ._
